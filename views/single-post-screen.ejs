<%-include ('./includes/header.ejs') %>
  
  <div class="container py-md-5 container--narrow">
    <div class="d-flex justify-content-between">
      <%-include ('./includes/flash.ejs') %>
      <h2><%=post.title%></h2>
      <% if (user && post.author.id.equals(id)){%>
        <span class="pt-2">
          <a href="/post/<%=post._id%>/edit" class="text-primary mr-2" data-toggle="tooltip" data-placement="top" title="Edit"><i class="fas fa-edit"></i></a>
          <form class="delete-post-form d-inline" action="/post/<%=post._id%>?_method=DELETE" method="POST">
            <button class="delete-post-button text-danger" data-toggle="tooltip" data-placement="top" title="Delete"><i class="fas fa-trash"></i></button>
          </form>
        </span>
      <%}%>
      
    </div>
      
    <p class="text-muted small mb-4">
      <a href="/profile/<%=post.author.username%>"><img class="avatar-tiny" src="https://gravatar.com/avatar/f64fc44c03a8a7eb1d52502950879659?s=128"></a>
      Posted by <a href="/profile/<%=post.author.username%>"><%=post.author.username%></a> on <%=post.createdAt.getDate()%>/ <%=post.createdAt.getMonth()+1 %> / <%=post.createdAt.getFullYear()%> </p>

    <div class="body-content">
      <%- filterUserHTML(post.body) %>
    </div>

  </div>
  
  <%-include ('./includes/footer.ejs') %>